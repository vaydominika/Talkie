// Add verified stroke-order image paths here as they are added to /public/stroke-order.
// A kana only exposes the viewer icon when it has an entry in this map.
export const strokeOrderImages: Record<string, string> = {};
