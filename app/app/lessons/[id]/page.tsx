import { notFound } from "next/navigation";
import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";
import { Button } from "@/components/ui/button";
export default async function LessonPage({ params }: { params: Promise<{ id: string }> }) { const user = (await auth())!.user; const lesson = await prisma.lesson.findUnique({ where: { id: (await params).id }, include: { blocks: { orderBy: { position: "asc" } } } }); if (!lesson) notFound(); async function complete() { "use server"; await prisma.lessonProgress.upsert({ where: { userId_lessonId: { userId: user.id, lessonId: lesson!.id } }, update: { completedBlocks: lesson!.blocks.length, completedAt: new Date() }, create: { userId: user.id, lessonId: lesson!.id, completedBlocks: lesson!.blocks.length, completedAt: new Date() } }); } return <article className="mx-auto max-w-3xl"><h1 className="text-3xl font-semibold">{lesson.title}</h1><div className="mt-8 space-y-8">{lesson.blocks.map(block => <section key={block.id}><h2 className="font-semibold">{block.title}</h2><p className="mt-2 whitespace-pre-wrap text-muted-foreground">{block.content}</p></section>)}</div><form action={complete} className="mt-10"><Button>Mark lesson complete</Button></form></article>; }
