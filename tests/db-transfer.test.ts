import {describe,expect,it} from "vitest";
import {DATABASE_TRANSFER_TABLES} from "@/lib/db-transfer";
describe("database transfer coverage",()=>{it("includes every learning-loop and social table in dependency order",()=>{for(const table of ["FocusStudySession","StudyPlan","StudyPlanItem","FriendRequest","FriendConnection","UserBlock","FriendNudge","Notification","FriendCommitment","GroupInvitation","GroupTimerInvitation","ListeningActivity","DailyQuestSet","DailyQuest","QuestEvent","WeeklyReflection","WeeklyGoalSchedule"])expect(DATABASE_TRANSFER_TABLES).toContain(table);expect(DATABASE_TRANSFER_TABLES.indexOf("StudyPlan")).toBeLessThan(DATABASE_TRANSFER_TABLES.indexOf("StudyPlanItem"));expect(DATABASE_TRANSFER_TABLES.indexOf("StudyPlanItem")).toBeLessThan(DATABASE_TRANSFER_TABLES.indexOf("FocusStudySession"));expect(DATABASE_TRANSFER_TABLES.indexOf("FriendRequest")).toBeGreaterThan(DATABASE_TRANSFER_TABLES.indexOf("User"))})});
